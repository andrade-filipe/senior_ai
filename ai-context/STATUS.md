# STATUS — Quadro Vivo

Atualizado a cada checkpoint humano. `software-architect` escreve; outros agentes leem.

## Legenda

- `planned` — bloco definido, ainda não iniciado.
- `ready` — pré-requisitos atendidos; pronto para iniciar no próximo kickoff.
- `in_progress` — em implementação.
- `review` — aguardando `code-reviewer`.
- `testing` — em `qa-engineer`.
- `done` — aprovado pelo usuário.
- `blocked` — travado; ver "Impedimentos".

## Fase atual: **Implementação — Onda 3 concluída**

Próxima fase: **Onda 4 (Bloco 0007 Docker Compose — ativa testes de integração skipados)**.

## Quadro de blocos

| Bloco | Status | Dono | R(s) | Notas |
|---|---|---|---|---|
| 0. Preparação — docs, agentes, guidelines | `done` | software-architect | — | Concluída em 2026-04-18 (commit `eca4bdf`). |
| 0.5. Design Técnico — 7 ADRs, requisitos, templates SDD, concretizações | `done` | software-architect | — | Concluída em 2026-04-18. |
| 1. Schema `AgentSpec` Pydantic + validação (transpilador) | `done` | transpiler-engineer | R01 | Concluída em 2026-04-18. 13 testes verdes; cobertura 97.73 %; `mypy --strict` limpo. Evidência em `docs/EVIDENCE/0001-agentspec-schema.md`. |
| 2. Transpilador MVP (Jinja2 + generator + CLI) | `done` | transpiler-engineer | R01 | Código concluído em 2026-04-19 (commit `cdb1776`). Templates + generator + CLI verdes. **Evidência pendente** (`docs/EVIDENCE/0002-transpiler-mvp.md` — capturar na consolidação de Onda 2). |
| 3. Servidores MCP OCR/RAG (mock, SSE) | `done` | adk-mcp-engineer | R02, R03, R11 | Onda 2b concluída 2026-04-19 — ADR-0001 (SSE) + ADR-0007 (rapidfuzz + CSV). Ambos serviços verdes (pytest + mypy). `code-reviewer` (Opus) aceito com 1 blocker (pii_mask bloqueante sob `wait_for`) corrigido inline via `asyncio.to_thread`. **Evidência pendente** (`docs/EVIDENCE/0003-mcp-ocr-rag.md` stub — capturar na consolidação). |
| 4. API FastAPI de agendamento + Swagger | `done` | fastapi-engineer | R04 | Onda 2b concluída 2026-04-19 — contrato em `docs/ARCHITECTURE.md`. 36 tests + 95 % cov + mypy limpo. `code-reviewer` (Opus) aceito com 3 blockers corrigidos inline: (1) `CorrelationIdMiddleware` resetava ContextVar antes do log (cid aparecia como "unknown"); (2) `TimeoutMiddleware` usava `asyncio.shield(...)` impedindo cancel do task; (3) taxonomia: `E_API_INTERNAL` + `E_API_PAYLOAD_TOO_LARGE` ausentes da master-table (acrescentados em `docs/ARCHITECTURE.md` + constante importada). **Evidência pendente** (`docs/EVIDENCE/0004-scheduling-api.md`). |
| 5. Camada PII (Presidio + BR recognizers) | `done` | security-engineer | R05 | Código concluído em 2026-04-19 (commit `b114e86`). Presidio + 4 BR recognizers + hard timeout; 62 pass / 1 xfail. **Evidência pendente** (`docs/EVIDENCE/0005-pii-guard.md` — capturar na consolidação de Onda 2). |
| 6. Agente ADK end-to-end | `done` | adk-mcp-engineer | R06 | Onda 3 concluída 2026-04-19. Transpilador emite `generated_agent/` (agent.py + __main__.py + logging_.py) consumindo MCPs OCR/RAG (SSE) e Scheduling API (OpenAPI via httpx). `security.make_pii_callback` liga Layer 2 do ADR-0003 ao ADK `before_model_callback`. 53 transpiler + 73 security + 28 generated_agent pass; mypy limpo. **Duas rodadas** de code-reviewer (Opus): 3 blockers + 8 majors (round 1) + 2 majors residuais (round 2) corrigidos inline. **Integration tests `@skip_integration` permanecem até Onda 4.** Evidência pendente (`docs/EVIDENCE/0006-generated-agent.md` stub — capturada no Bloco 0008). |
| 7. Dockerfiles + `docker-compose.yml` | `planned` | devops-engineer | R07 | `uv pip install --system` (ADR-0005). |
| 8. E2E + evidências + README + Transparência | `planned` | qa-engineer + software-architect | R08, R09, R10, R12 | Última fase; fecha o desafio. |

## Impedimentos

*(nenhum no momento)*

## Histórico de checkpoints

- **2026-04-18** — Preparação concluída (commit `eca4bdf`): 8 subagentes, separação `docs/` vs `ai-context/`, WORKFLOW, GUIDELINES, ARCHITECTURE (esqueleto), CLAUDE.md, `.gitignore`, git init + remote.
- **2026-04-18** — Design Técnico concluída (commit `3c35ad2`): 7 ADRs aceitas; `docs/REQUIREMENTS.md` com R01..R12; `docs/specs/README.md` com templates SDD; `docs/ARCHITECTURE.md` com schema Pydantic + tool signatures + PII entities + error taxonomy + log format; `ai-context/WORKFLOW.md` com ciclo SDD+TDD de 9 passos; 8 agentes anotados com ADRs aplicáveis + papel no ciclo; `CLAUDE.md` com stack fechada.
- **2026-04-18** — Auditoria crítica do design (laudo em `ai-context/references/DESIGN_AUDIT.md`): 10 claims externos + 7 checagens internas + 5 riscos. Correções factuais aplicadas inline: Gemini `2.0-flash` → `2.5-flash` (descontinuado); `MCPToolset(SseConnectionParams)` → `McpToolset(StreamableHTTPConnectionParams)` (ADK renomeou); Presidio BR recognizers documentados como custom (lib não oferece); URL docs ADK migrada para `adk.dev/`. Nenhuma mudança de mérito → zero ADR nova.
- **2026-04-18** — Hardening pass (ADR-0008 "Robustez de validação"): política centralizada de taxonomia de erros `E_*`, shape canônico `{code, message, hint, path, context}`, guardrails (caps de listas/strings/bytes, timeouts), correlation_id, regra "no-PII-in-logs". 20 novos ACs (P1) distribuídos entre os 8 blocos, com ~34 novos tests `[DbC]` em `tasks.md`. Extensões em `docs/ARCHITECTURE.md` (seção "Robustez e guardrails" com tabela consolidada de códigos + guardrails + timeouts), `docs/specs/README.md` (template extendido), `.claude/agents/code-reviewer.md` (checklist ADR-0008) e `ai-context/GUIDELINES.md` (pointers). Pendente: kickoff do Bloco 1 (aguarda autorização do usuário).
- **2026-04-18** — Onda 1 concluída (Bloco 0001 — AgentSpec Schema): `transpiler/` pacote inaugurado, 13 testes unitários cobrindo AC1–AC13 (AC13 parcial — CLI test diferido ao Bloco 0002), cobertura 97.73 %, `mypy --strict` limpo. `code-reviewer` (Opus) aprovou com ressalvas menores — todas as 11 tratadas (5 fixes aplicados, 6 já cobertas pelo primeiro pass). Ambiente: `uv 0.11.7` + Python 3.12.13 gerenciado via `uv python install`. Dev deps migradas de `[project.optional-dependencies]` para `[dependency-groups]` (PEP 735) para que `uv sync` instale tudo por default. Evidência: `docs/EVIDENCE/0001-agentspec-schema.md`.
- **2026-04-19** — Onda 2a concluída (Blocos 0002 Transpiler MVP + 0005 PII Guard) em sequência narrativa de 4 commits: `8ac971f` docs(contracts) alinhou spec de PII com ADR-0008 (hard timeout 5 s + `allow_list` cap 128); `a0eb172` docs(guidelines) formalizou a regra "commits contam história"; `cdb1776` entregou o transpilador MVP (templates Jinja2 + generator + CLI); `b114e86` entregou a camada PII (Presidio + 4 BR recognizers com checksum + hard timeout + allow_list + idempotência). Bugs capturados e corrigidos durante integração: `analysis_explanation=None` crasheava o `LemmaContextAwareEnhancer` (fix: in-place mutation do `score` em vez de construir novo `RecognizerResult`); idempotência falhava em `<EMAIL>`→`<<PERSON>>` (fix: regex filter de spans placeholder antes de mascarar); CPF inválido mascarado (fix: `_SCORE_INVALID` 0.3→0.1 + `score_threshold=0.5` no `analyzer.analyze()`). **Arquivos de evidência `0002` e `0005` ficaram pendentes** — serão capturados junto com `0003` e `0004` na consolidação de Onda 2.
- **2026-04-19** — Onda 2b iniciada em paralelo: Bloco 0003 (MCP OCR+RAG, 21 ACs) delegado ao `adk-mcp-engineer`; Bloco 0004 (Scheduling API, 17 ACs) delegado ao `fastapi-engineer`. Ambos retornaram no mesmo dia com implementação completa mas **sem auto-verificação** (agents sem permissão Bash). Entregues: `ocr_mcp/` + `rag_mcp/` (115 linhas CSV SIGTAP ≥ 100 AC6) + `scheduling_api/` (17 ACs). Próximo passo: orquestrador roda `uv sync` + `uv run pytest -v` + `uv run mypy` em cada serviço, então `code-reviewer` (Opus) + commits em sequência narrativa.
- **2026-04-19** — Onda 3 concluída (Bloco 0006 Generated Agent). Integração ADK × MCP × API em um pacote `generated_agent/` emitido pelo transpilador, mais `security.make_pii_callback` como Layer 2 do ADR-0003. Duas rodadas de code-reviewer (Opus) foram necessárias: (i) round 1 identificou 3 blockers (imagem enviada como base64 em Part de texto → PII callback dispararia `E_PII_TEXT_SIZE` e silenciosamente mascararia fallback; `X-Correlation-ID` não era propagado em nenhuma chamada de saída apesar do docstring; `guardrails.pii.allow_list` do spec era descartado — template hard-coded `[]`) + 8 majors (ex.: `_exit_error` sem `correlation_id`, `_parse_runner_output` com `except Exception` genérico, taxonomia sem `E_AGENT_INPUT_NOT_FOUND`, Dockerfile `COPY ../security` fora do contexto de build, port drift `:8003` vs `:8000` em `spec.example.json`). Todos corrigidos inline + testes adicionados. (ii) round 2 revelou 2 majors residuais — `root_agent = _build_agent("boot")` em escopo de módulo ainda disparava `httpx.get` bloqueante no import (fix: `__getattr__` lazy); `test_agent_timeout_300s` só chamava `_exit_error` sinteticamente, não exercia cancelamento real (fix: novo `test_run_agent_closes_toolsets_on_timeout` com generator stub que dorme além do `wait_for(0.1)` e asserta que `close()` de ambos toolsets rodou no `finally`). Também pegou MINOR de mime-type hardcoded `image/png` (fix: `mimetypes.guess_type(image_path)` com fallback). Commits em sequência narrativa: `c2836a9` bundled round 1 (violação da regra "commits contam história" — agent commitou antes do orquestrador particionar) + 5 commits individuais para round 2 e órfãos (`security/__init__.py` expose + `py.typed`, bytes-based schema cap, templates, round-2 generated_agent, .env.example + EVIDENCE). Dependências novas no `generated_agent`: `google-adk>=1.0.0,<2`, `mcp[cli]>=1.0,<2`, `httpx>=0.27,<1`. Integration tests (`@skip_integration` — 11 tests) permanecem skipados até Bloco 0007 montar o docker compose. Evidência consolidada (`0002/0003/0004/0005/0006`) capturada no Bloco 0008.
- **2026-04-19** — Onda 2b concluída. Três rodadas de correção foram necessárias entre a entrega dos agents e o verde final. (i) Integração: `mcp[cli]` 1.27 moveu `host/port` do `FastMCP.run()` para o construtor → ajuste nos 2 servers + `__main__`. `_tool_manager.tools` renomeado para `_tools` → teste de registro usa o nome atual e faz `pytest.skip` se futura versão renomear. (ii) Log: `_JsonFormatter` lia `record.extra` (atributo inexistente no stdlib `logging`); chamadas passavam `extra={"message": ...}` que colidia com campo reservado. Rewrite do formatter coletando fields não-reservados de `record.__dict__` + remoção de `"message"` dos call-sites. (iii) `code-reviewer` (Opus) revelou 4 blockers que os testes não pegaram: `CorrelationIdMiddleware` resetava ContextVar antes do log (cid="unknown"); `TimeoutMiddleware` usava `asyncio.shield(...)` dentro do `wait_for` (task leak); `pii_mask` rodava sincronamente sob `wait_for` em `ocr_mcp._do_ocr` (timeout não preemptava); `E_API_INTERNAL` + `E_API_PAYLOAD_TOO_LARGE` não estavam na master-table de `docs/ARCHITECTURE.md § Códigos de erro consolidados` (acrescentados + constante importada de `errors.py`). Pós-fix: 3 stacks verdes (`scheduling_api` 36 pass, `ocr_mcp` 25 pass, `rag_mcp` 34 pass + mypy limpo em todas). Evidência `0003/0004` pendente junto com `0002/0005` — consolidação na Onda 5.
